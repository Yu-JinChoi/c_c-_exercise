#include<stdio.h>
#define start 3;
#define mon 31;

int main()
{
	int date,day;
	for(date=0; date<3; date++)
	printf(" ");
	for(date=1;date<=31;date++){
		if(day==7)
		{day==0;
		printf("\n");
		} day++;
		printf("%2d",date);
	}return 0;
}
